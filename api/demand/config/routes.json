{
  "routes": [
    {
      "method": "GET",
      "path": "/demands",
      "handler": "Demand.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/demands/count",
      "handler": "Demand.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/demands/:id",
      "handler": "Demand.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/demands",
      "handler": "Demand.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/demands/:id",
      "handler": "Demand.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/demands/:id",
      "handler": "Demand.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
